{
   "version": 1,
   "name": "mmern-backend",
   "builds": [
      { "src": "server.js", "use": "@vercel/node" }
   ],
   "routes": [
      { "src": "/(.*)", "dest": "/app.js" }
   ]
  "rewrites": [{ "source": "/api/(.*)", "destination": "/api" }]
}
